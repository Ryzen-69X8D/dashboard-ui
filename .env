# The URL where your autoData FastAPI server is running
BACKEND_API_URL=http://localhost:8000

# The proxy endpoint your Node.js server exposes to the frontend
BACKEND_API_ENDPOINT=/api/ml/predict

# Backend Port (Node.js)
PORT=3000